# nilamind ProGuard rules.
#
# nilamind ships no third-party reflection-based libraries and
# no native code. The Compose runtime handles its own rules via
# the AGP-managed consumer files. We keep this file as the
# standard AGP placeholder so the release build type's
# `proguardFiles` block resolves cleanly.
