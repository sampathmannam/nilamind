node_modules/
build/
dist/
dist-server/
dev-dist/
coverage/
.DS_Store
*.log
.env*
!.env.store

# Android build outputs (keep the native project, ignore generated/build artifacts)
android/build/
android/app/build/
android/.gradle/
android/.idea/
android/captures/
android/local.properties
android/app/release/

# Packaged apps & signing material — NEVER commit
*.apk
*.aab
*.keystore
*.jks
key.properties

# On-device LLM weights (MediaPipe .task / GGUF / LiteRT) — large + never committed; side-loaded per device
*.task
*.gguf
*.litertlm
*.bin

# On-device STT model — runtime-downloaded (onDeviceAssets.ts), not bundled; keeps the source tree free
# of prebuilt binaries (required for F-Droid's from-source build scanner). The ONNX Runtime WASM binary
# was reverted to bundled 2026-07-16 (see crisisEmbedder.ts) — safety-critical, correctness over scanner
# cleanliness until the download path is re-verified.
public/models/vosk-model-small-en-us-0.15.tgz

# Capacitor-synced web assets (generated from dist/ by `cap sync`)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json

# Python research scripts — compiled bytecode (research/s1-eval, research/rs-spike)
__pycache__/
*.pyc

# Tooling/session artifacts
.claude/

# superpowers subagent-driven-development scratch (briefs, reports, diffs, ledger)
.superpowers/
.aider*

# Local model-training pipeline (teacher generation, QLoRA, eval gates) — kept on disk for future
# training but NOT published with the app. Includes pulled crisis datasets (Aegis/CounselChat/Mendeley)
# that were cleared for training/eval but are NEVER committed (sensitive crisis text lives with the model).
research/

# Private audit report — documents §9/crisis-bypass details; never commit/push
OPENCODE_FIX_REPORT.md

# Local audit reports — contain §9 crisis-bypass detail, never commit/publish
AUDIT_*.md

phone_log.txt
phone_screenshot.png
/launch-kit/
