# Gradle-wide properties for nilamind.
#
# androidx is required because the project pulls
# core-ktx, lifecycle, activity-compose, and the entire
# Compose BOM. The jetifier flag is left at the AGP
# default (off) because no support-library code is
# pulled in here.
android.useAndroidX=true
