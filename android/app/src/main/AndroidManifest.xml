<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- nilamind://voice — the quick voice check-in widget's deep link. Routes straight
                 to the Nila tab via App.tsx's appUrlOpen listener (see QuickVoiceWidget.java). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nilamind" android:host="voice" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Home-screen widget (Phase 7): gentle streak + one-tap check-in. -->
        <receiver android:name=".StreakWidget" android:exported="true" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/streak_widget_info" />
        </receiver>

        <!-- Quick voice check-in widget — answers a Product Hunt launch-day feature request
             (docs/FEATURES_PLAN.md "User-requested" U1). -->
        <receiver android:name=".QuickVoiceWidget" android:exported="true" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/quick_voice_widget_info" />
        </receiver>

        <!-- Keep-resident foreground service: holds the process so the ~2.3GB model + its warmed page cache
             aren't evicted between sessions (docs/NILA_SPEED_PLAN.md keep-resident lever). specialUse because no
             standard FGS type fits "keep an on-device model in memory"; honest subtype declared below. -->
        <service
            android:name=".ModelResidentService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the on-device AI model resident in memory so replies stay fast on slow-storage devices" />
        </service>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Behaviour Intelligence (plan Part 11/12) — metadata only, on-device, no content. -->
    <!-- PACKAGE_USAGE_STATS removed for Play Store compliance; re-enable for sideload builds only. -->
    <!-- <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" /> -->

    <!-- Voice input (STT), plan Phase 4 — microphone + access to a speech recognizer. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Required by Chromium WebView media stack for getUserMedia (WebRTC audio). -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Keep-resident foreground service (docs/NILA_SPEED_PLAN.md): hold the process so the model isn't evicted. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 13+: for the gentle, low-priority foreground-service notification to be visible. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
